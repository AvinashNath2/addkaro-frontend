# Copy this file to .env.local and fill in the values
# The Vite dev server proxies /api → VITE_API_TARGET automatically (see vite.config.ts)
# You only need this if you want to override the proxy target

VITE_API_TARGET=http://localhost:8082
